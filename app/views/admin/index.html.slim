h2 To get a CSV formatted version of this file, go to /tony-secret-back-door.csv

table
  thead
    tr
      th Session ID
      th Tester Name
      th Original order
      th Response (1: top, 2: bottom)
      th Date/Time
  tbody
    - @comparison.each do | field|
      tr
        td= field.session_id
        td= field.session.tester_name
        td= field.original_id
        td= field.tester_response
        td= field.updated_at
